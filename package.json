{
  "name": "@papercusp/rate-limit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generic two-bucket rate limiter (soft refundable + hard lockout) over an injected BucketStore. Pure algorithm, zero domain coupling, zero runtime deps.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
